{
    "blueprint": {
        "name": "online_beverages",
        "type": "kubernetes",
        "config": {
            "kubernetes": {
                "configType": "yaml",
                "yaml": "{{blueprint_yaml}}"
            }
        },
        "visibility": "private"
    }
}