{
    "blueprint": {
        "name": "{{blueprint_name}}",
        "type": "kubernetes",
        "config": {
            "kubernetes": {
                "configType": "yaml",
                "yaml": "{{blueprint_yaml}}"
            }
        },
        "visibility": "private"
    }
}